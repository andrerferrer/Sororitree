{
  "senderNickname": "<%= current_user.nickname %>",
  "message":  "<%= render('message_broadcasted', message: message).gsub("\n", " ").html_safe %>"
}

<%# Message still breaks if we write it in two lines %>
<%# e.g.: 
  "Hello, world" will not break;
  "Hello
  World" will %>